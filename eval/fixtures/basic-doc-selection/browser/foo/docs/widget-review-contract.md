# Widget Review Contract

Review invariant: widget IDs must remain stable across restarts.

If a patch changes a persisted widget ID to a generated runtime ID, the review must mention:

REVIEW_CONTRACT_STABLE_WIDGET_IDS
